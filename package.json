{
  "name": "f8-htmlcss-project-02.1",
  "version": "1.0.0",
  "description": "HTML & CSS project using Sass for styling",
  "main": "index.js",
  "scripts": {
    "dev": "sass --watch assets/scss:assets/css --style=expanded --source-map",
    "build": "sass assets/scss:assets/css --style=compressed --no-source-map --no-error-css --no-charset",
    "start": "npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
